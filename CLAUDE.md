# Claude Code Instructions

Use `AGENTS.md` as the primary project instruction file.

Full, tool-neutral rules live in `docs/agent-guidelines/`. Do not create a
Claude-only rule unless the behavior is specific to Claude Code. For project
rules, update the canonical guideline first and keep this adapter minimal.
