## Changes

- Add local ASR serving for Whisper, FunASR, Qwen3-ASR, GLM-ASR, SenseVoice, and Paraformer models with OpenAI-compatible transcription APIs.
- Add audio upload and microphone recording in Chat, plus ASR-specific Dashboard API examples and Library run parameters.
- Fix ASR runtime setup and local model import for PyTorch/FunASR weights, including shared `ai-runtime`, bundled ffmpeg, and stable keep-alive worker reuse.
