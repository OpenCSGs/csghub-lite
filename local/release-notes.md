## Changes

- Fix marketplace ASR model detection from supported architectures, supported model names, and task metadata so FunASR models show local ASR inference support.
- Add a download action and progress state to marketplace model detail dialogs.
- Keep downloads alive while navigating inside the app by using SPA navigation, and show interrupted downloads as resumable instead of surfacing stream errors.
